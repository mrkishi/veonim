const { src, same } = require('../util')

const api = src('vscode/api')

describe('vscode api - extensions', () => {
  describe('var', () => {
    it('all')
  })

  describe('func', () => {
    it('getExtension')
  })
})
