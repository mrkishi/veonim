const { src, same } = require('../util')

const api = src('vscode/api')

describe('vscode api - scm', () => {
  describe('var', () => {
    it('inputBox')
  })

  describe('func', () => {
    it('createSourceControl')
  })
})
