const { src, same, globalProxy } = require('../util')

const api = src('vscode/api')

describe('vscode api - env', () => {
  describe('var', () => {
    it('appName')
    it('appRoot')
    it('language')
    it('machineId')
    it('sessionId')
  })
})
