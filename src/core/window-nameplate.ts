export default () => {
  // TODO: hyperapp component
  // copy structure/styles from windows.ts
  const element = document.createElement('div')
  const update = () => {
    // TODO: hook up update to hyperapp action to update nameplate data
  }
  return { element, update }
}
